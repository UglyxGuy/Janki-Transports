export interface ServiceCard {
  icon: string;
  title: string;
  description: string;
}

export interface Statistic {
  number: string;
  label: string;
}

export interface NavLink {
  name: string;
  path: string;
} 